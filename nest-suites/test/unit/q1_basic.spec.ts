// Level 1 — 개념 이해 (README.md 참고)
// 코드로 증명할 대상이 없는 개념 문답. 노트나 이 파일 주석에 직접 답을 채워 넣는다.

describe('Level 1 — 개념 이해', () => {
  it.todo(
    'Q1. Suites에서 Stub, Mock, Mocked<T>의 관계를 한 문장씩 정의할 수 있는가? ("Mock은 ___의 집합이다")',
  );

  it.todo(
    'Q2. TestBed.solitary()와 @nestjs/testing의 Test.createTestingModule()은 내부 동작이 근본적으로 어떻게 다른가? (힌트: 하나는 NestJS 컨테이너를 실제로 부트스트랩하고, 하나는 하지 않는다)',
  );

  it.todo(
    'Q3. solitary와 sociable의 차이를 "무엇이 진짜이고 무엇이 목인가"로 설명. sociable은 왜 여전히 단위 테스트로 분류되는가? (힌트: I/O 경계)',
  );

  it.todo(
    'Q4. "단위 테스트는 Suites, 모듈 구성 검증은 createTestingModule" — Suites로는 검증할 수 없는 것이 무엇이길래 이렇게 나뉘는가? (힌트: 가드, 모듈 imports, 프로바이더 등록 누락)',
  );
});
